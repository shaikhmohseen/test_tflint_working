name: linter
on:
  push:
    branches: [ main ]
  pull_request:
    

jobs:
  tflint:
    runs-on: ubuntu-latest



    steps:
    - uses: actions/checkout@v1
      name: Checkout source code

    - uses: hashicorp/setup-terraform@v1
      with:
        terraform_version: 1.0.5

    - uses: terraform-linters/setup-tflint@v1
      name: Setup TFLint
      with:
        tflint_version: v0.26.0

    - name: Run TFLint
      run: tflint -f compact

    - name: Terraform fmt
      id: fmt
      run: terraform fmt -list=true -write=false -diff=true -check=true -recursive
      continue-on-error: false

    - uses: ravsamhq/notify-slack-action@v1
      if: always()
      with:
        status: ${{ job.status }} # required
      env:
        SLACK_WEBHOOK_URL: https://hooks.slack.com/services/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

    #    - name: Send mail
#      if: always()
#      uses: dawidd6/action-send-mail@v3
#      with:
#        server_address: smtp.gmail.com
#        server_port: 465
#        username: ${{ secrets.EMAIL_USERNAME }}
#        password: ${{ secrets.EMAIL_PASSWORD }}
#        subject: Github Actions job result
#        to: shaikhmr7786@gmail.com,mohseen.shaikh@medly.com
#        from: Mohseen Shaikh # <user@example.com>

