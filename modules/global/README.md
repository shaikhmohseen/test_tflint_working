# [WIP] Global modules & resources

This directory is to store configuration for account level resources

- SSM parameters defined for an AWS account to be used for multiple services, apps, etc.
Example:
  - Okta auth URL
  - Okta base URL
